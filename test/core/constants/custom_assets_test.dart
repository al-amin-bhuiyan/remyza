import 'package:flutter_test/flutter_test.dart';

// [UNIT] Tests custom assets constants

void main() {
  test('CustomAssets should not be instantiable and only contain static properties', () {
    // Add tests for the constants if required
  });
}

